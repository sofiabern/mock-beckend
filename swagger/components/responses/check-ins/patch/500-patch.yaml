description: 'Check-in not found'
content:
  application/json:
    schema:
      type: object
      required:
        - status
        - message
        - errors
      properties:
        status:
          type: integer
          example: 500
        message:
          type: string
          example: Check-in not found
        errors:
          type: array
          items: {}
          example: []

